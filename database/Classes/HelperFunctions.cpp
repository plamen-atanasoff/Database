#include "HelperFunctions.h"

#include "Optional/Optional.hpp"

//std::vector<int> getRecordsPositions(const Column* col, const String& value, size_t recordsCount)
//{
//	std::vector<int> res;
//
//	// assert values size in col == recordsCount
//	for (int i = 0; i < recordsCount; i++) {
//		if (col->getValue(i) == value) {
//			res.push_back(i);
//		}
//	}
//
//	return res;
//}
